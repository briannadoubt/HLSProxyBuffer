**Next steps**

- [x] Add and test `FeedBufferController` so visible rows keep playing, neighbors stay warm, and off-screen players release memory quickly.
- [ ] Add a standalone reference feed that uses the curated sources in `StreamSources.md`.
- [ ] Consider layering in feed-level controls (mute/like) or diagnostics (buffer depth graphs) once the reference feed exists.
